# Mini-ChatGPT-with-Static-Long-Dataset
A Mini ChatGPT with a static long dataset is a small chatbot model that generates responses based on a fixed, pre-written dataset instead of learning in real time
